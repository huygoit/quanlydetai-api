/**
 * Trước đây map rank/quartile → mã leaf. Đã bỏ: publication / project dùng research_output_type_id trực tiếp.
 * File giữ lại để tránh nhầm import cũ; logic KPI nằm ở kpi_engine/*_strategy.ts.
 */

export {}
